# AID Rummikub — Claude 작업 가이드

2인 루미큐브(조커 없음, 색 4 × 숫자 13 × 2벌 = 104타일)에서 그리디 ILP 베이스라인을
이기는 에이전트를 만드는 프로젝트. 상세 게임 룰·구조 설명은 `README.md` 참조.

**목표 제약 (2026-07-07 확정): 최종 에이전트는 학습된(learned) 에이전트여야 함.**
탐색 에이전트(fair combo)는 최종 제출물이 아니라 선생(증류 데이터 생성기) 역할.
합법성/후보 생성은 솔버가 담당해도 무방하나, 수 선택 결정은 네트워크가 해야 함.
단계적 요건: 수준 2(하이브리드 — 미드게임은 네트워크, 손패 ≤5 종반 DFS 허용 +
네트워크 기여 ablation 증명) 먼저, 최종 목표는 수준 3(순수 네트워크).
**R10 전체 전략·단계·리스크는 `ROADMAP.md` 참조** (2026-07-07 승인).

## 환경 / 실행

- conda env: `rummikub` (`/opt/homebrew/Caskroom/miniconda/base/envs/rummikub`)
- 패키지는 **pip이 아니라 `conda install -c conda-forge`** 로 설치할 것.
- pulp 솔버: conda-forge pulp에는 CBC 바이너리가 없음 → `PULP_CBC_CMD` 불가.
  `COIN_CMD`(서브프로세스) 또는 `HiGHS`(highspy, in-process — R8부터 기본) 사용.
- 학습: `python train_ppo.py --tag rX ...`, 평가: `python eval_ppo.py`, 미러 평가: `eval_mirror.py`(R8).

## 코드 지도

| 파일 | 역할 |
|---|---|
| `rummikub_solver.py` | 한 턴 최적화 ILP (`solve`), 후보 다양화 (`solve_many`) |
| `rummikub_env.py` | 게임 상태(덱/손패/테이블) + 솔버 래퍼 |
| `ppo_env.py` | Gymnasium 환경. PPO vs 상대(그리디 ILP/랜덤), 보상, 관측 |
| `ppo_model.py` | ActorCritic: state 인코더 + 후보 인코더 + score head |
| `train_ppo.py` | 자체 PPO 루프 (SB3는 VecEnv만 사용) |
| `eval_ppo.py` | 정책 평가 (det/stoch/random × ILP/random 상대) |
| `rollout_agent.py` | (R8) determinized rollout 정책 — 학습 없는 시뮬레이션 기반 수 선택 |
| `eval_mirror.py` | (R8) 미러 페어(같은 덱, 자리 교대) 저분산 평가 |

## R1–R7 요약과 진단 (2026-07 검토)

R5~R7 모두 그리디 ILP 상대 승률이 40~60%에서 진동, 추세 없음. 원인은 구조적:

1. **보상이 그리디 모방을 가르침**: `+0.1×낸 타일`, `드로우 -0.5` dense 보상의 최적해는
   상대(그리디)와 동일한 정책 → 승률 50%가 고정점.
2. **결정 기회 희박**: 턴의 53%가 forced draw(후보 0개), chosen draw ≈ 0.
   실제 선택은 평균 4.6개 후보 중 하나뿐.
3. **에피소드 보상 = 운 지표**: 보상의 대부분이 통제 불가능한 forced draw 페널티.
   best 체크포인트도 avg_reward 기준이라 사실상 무작위 선택이었음.
4. **데이터/속도 부족**: R7 전체 ≈ 3,100 에피소드, 5.2 steps/s.
   병목은 턴당 수십 회의 CBC 서브프로세스 spawn (특히 종료 시점의 무의미한 후보 생성).
5. **평가 분산**: 50판 평가는 오차 ±14%p → 라운드 간 비교가 무의미했음.

## 외부 연구 조사 결과 (2026-07, Haiku 리서치)

- **Rummikub 한 턴 최적화는 다항 시간에 풀림** — van Rijn & Takes 2016,
  https://arxiv.org/pdf/1604.07553. ILP를 DP로 대체 가능(장기 속도 대책).
  전체 게임 전략(낼까/참을까) 연구는 학술적 공백.
- **Maven(Scrabble)**: 정적 평가 대신 후보별 determinized rollout(상대 타일 샘플링 후
  시뮬레이션)으로 세계 챔피언급 달성. https://www.sciencedirect.com/science/article/pii/S0004370201001667
- **PIMC/ISMCTS**: 히든 정보 게임에서 determinization 샘플링 평가가 표준.
  strategy fusion 한계 있으나 루미큐브류(정보 노출이 수동적)엔 잘 맞는 편.
- **Suphx(마작)**: oracle guiding(완전정보 학습 후 특권 정보 제거) + global reward
  prediction. https://arxiv.org/abs/2003.13590
- **DouZero+**: 상대 손패 예측 헤드를 정책에 결합해 승률 향상. https://arxiv.org/abs/2204.02558
- **고정 상대 학습의 함정**: 고정 그리디 상대 + dense 보상 → ~50% 정체가 전형.
  처방은 자가대전/체크포인트 풀(과거 체크포인트 ~75% + 고정 상대 ~25%).
- **평가 분산 감소**: duplicate(미러) 딜 + AIVAT. 포커에서 44× 적은 판수로 유의성 달성.
  https://ojs.aaai.org/index.php/AAAI/article/view/11481

## R8 로드맵 (승인됨)

1. **속도 수술**: 종료 시 후보 생성 스킵, `solve_many` 내 후보 리스트 재사용,
   랜덤 상대 후보 수 축소, `pulp.HiGHS`(in-process) 기본 솔버화.
2. **평가 교정**: `eval_mirror.py` — 같은 시드 덱으로 자리 바꿔 2판(페어) 평가,
   페어 단위 통계. train의 best 선택을 avg_reward → expected_score/승률 기반으로.
3. **Rollout 에이전트**: ILP 후보별로 미확인 타일(104 − 내 손 − 테이블)에서 상대
   손패를 k회 샘플링(determinization) → 그리디 vs 그리디 depth 제한 롤아웃 →
   리프 평가(승패 ±큰값, 아니면 상대손패−내손패) 평균이 최고인 후보 선택.
   **학습 없이 그리디 대비 우위 여부를 먼저 검증하는 것이 R8의 핵심 실험.**
4. (R9 후보) rollout 정책을 네트워크에 증류(expert iteration), 관측에 미확인 타일
   52차원 추가, 크리틱에 완전정보(비대칭 actor-critic), 자가대전 풀.

## R8 진행 기록 (2026-07-02)

- **HiGHS 전환 완료**: solve당 55.6ms(CBC 서브프로세스) → 1.4ms (~40배).
  종료 시 후보 생성 스킵 포함, env 스텝 ~1.5s → ~0.1s.
- **버그 발견·수정**: `ppo_env.step()`이 `initial_meld_value=0`일 때도 첫 수를
  `append_to_table=True`로 적용 → 테이블 재배열 후보(ignore_table=False)와 결합 시
  타일 복제로 상태 오염. R7은 meld=30으로 학습해 우연히 회피. 수정 +
  `apply_solution`에 복제 감지 가드 추가. **meld=0으로 돌린 과거 결과는 무효.**
- **미러 평가 sanity 통과**: greedy vs greedy 30페어 = 승률 46.7%, 28/30 페어가
  완전 미러(WL/LW). 잔여 비대칭은 동점 최적해 tie-break 차이(정상).
- **rollout v1 (det=8, horizon=12, cap=6) 결과: 그리디에 패배** —
  40페어 승률 41.2%, pair net -0.90±0.60. 얕은 호라이즌 + 리프 평가(타일 차)로는
  신호가 없어 "노이즈만큼 그리디에서 이탈 → 손해" (naive PIMC의 전형적 실패).
- **rollout v2 (greedy-default margin=1.0, horizon=24, cap=4) 결과: 그리디와 동급** —
  같은 시드 40페어 승률 47.5%, pair net -0.47±0.54 (greedy sanity 46.7%와 통계적
  동일). margin 가드로 "그리디 이하"는 벗어났으나 우위 신호는 없음.
- **오라클 실험 확정 (R8 핵심 결론)** (rollout에 진짜 상대 손패·덱 순서 제공,
  det=1, horizon=60, cap=6): **40페어 승률 88.8% (71승 9패, WW 31페어),
  pair net +6.28±0.54.** 완전정보 1-ply rollout만으로 그리디를 압살함.
  → **edge는 크게 존재하고, 병목은 탐색이 아니라 히든 정보(상대 손패) 추정.**
  균등 무작위 determinization은 정보가 0이라 신호를 못 만듦 (v1/v2가 증명).
- **속도 추가 개선**: HiGHS threads=1 + 유효세트 need-Counter 사전계산
  (solve 1.4→0.87ms), `eval_mirror.py --workers N` 페어 병렬화 (~5-7배).
- **세미 오라클 실험 (진짜 상대 손패 + 샘플링 덱, det=8, horizon=60): 승률 47.5%,
  pair net -0.23±0.55 — fair rollout v2와 동일.** 손패를 완벽히 알아도 이득이 0.
  → **full oracle의 88.8%는 전부 덱 순서 예지(자기 미래 드로우를 아는 것)에서
  왔고, 이는 원리적으로 예측 불가능. "상대 손패 예측기" R9 가설은 기각.**
- **R9 방향 재설계 필요**: 손패 추정 투자는 이 아키텍처(ILP 후보 + 그리디 플레이
  아웃 + 1-ply)에서 회수 불가. 남은 가설: (1) 후보 생성이 "최대로 내기" 계열뿐
  이라 방해(blocking)/테이블 셰이핑 수가 아예 없음 — 상대를 막는 수를 후보에
  추가해야 손패 지식이 쓸모를 가질 수 있음, (2) meld=0 2인 룰 자체가 그리디로
  거의 포화된 운 게임일 가능성 — meld=30 룰(결정 밀도 higher)에서 재측정,
  (3) 엔드게임(덱 소진 임박) 한정으로는 덱 불확실성이 작아져 손패 지식이 유효할
  수 있음 — 엔드게임 한정 세미 오라클로 검증.

## meld=30 룰 재측정 (2026-07-03, 로그: `R8/meld30_*.log`)

같은 시드(2000~2039) 40페어, 초기등록 30 룰. meld=0과 비교:

| 실험 | meld=0 (승률 / pair net) | meld=30 (승률 / pair net) |
|---|---|---|
| greedy sanity | 46.7% / -0.40±0.25 | 51.2% / +0.07±0.25 |
| fair rollout v2 | 47.5% / -0.47±0.54 | 52.5% / +0.40±0.44 |
| semi-oracle (손패만) | 47.5% / -0.23±0.55 | **56.2% / +0.30±0.44** |
| full oracle (손패+덱) | 88.8% / +6.28±0.54 | 87.5% / +5.25±0.43 |

**판정**: (a) meld=30에서 순서가 sanity < fair < semi로 정렬 — meld=0에서 손패
지식 가치가 0이던 것과 달리, 표준 룰에서는 fair +1.3%p, semi +5%p의 **약한 양의
신호**. 아직 40페어로는 비유의(1σ 이내). (b) full oracle 상한은 룰 무관 ~88%
(덱 예지 지배). (c) 가설 2(meld=30에 결정 밀도/skill 존재)를 지지하는 방향이며,
pre-meld 드로우 신호("30을 못 만드는 손"이라는 강한 제약)가 semi 우위의 유력한
원천. **160페어 확장으로 유의성 확정이 다음 단계.**

## 160페어 확장 최종 판정 (2026-07-04, R8 마감)

신규 120페어(시드 2040~2159) + 기존 40페어 풀링, meld=30:

| 실험 | 40페어 | 신규 120페어 | **풀링 160페어 (320게임)** |
|---|---|---|---|
| greedy sanity | 51.2% / +0.07 | 47.9% / -0.18±0.11 | **48.8% / -0.12±0.10** |
| fair rollout v2 | 52.5% / +0.40 | 50.4% / -0.04±0.29 | **50.9% / +0.07±0.24** |
| semi-oracle | 56.2% / +0.30 | **55.8% / +0.15±0.29** | **55.9% / +0.19±0.24** |

- **semi-oracle 신호는 실재**: 독립 표본에서 56.2% → 55.8%로 재현 (fair·sanity는
  회귀). 승률 기준 semi−sanity = +7.2%p (~2σ). 단 pair net 마진은 작음(+0.3) —
  근소한 차이로 이기는 형태.
- **fair rollout 자체는 무가치 확정**: 50.9% vs 기준선 48.8% (0.5σ). 손패 정보
  없는 rollout 기계는 아무것도 더하지 못함.
- **R9 방향 확정 — 예측 노선**: fair(50.9%)→semi(55.9%)의 5%p 갭이 손패 추정으로
  회수할 예산. 1단계: 정보 일관 determinization (그리디 상대의 드로우 = "낼 조합
  0개", pre-meld 드로우 = "30 미달 손"이라는 결정론적 제약으로 샘플 기각) —
  학습 없이 갭의 회수율 측정. 2단계: 상대 손패 분포 예측 학습(자가대전 데이터
  지도학습) → determinization을 예측 분포에서 샘플링. 3단계: 증류(expert
  iteration). 블로킹 후보 노선은 보류.
- 운영 메모 추가: `caffeinate -is`는 **배터리 구동 + 클램셸에는 무력** (-s는 AC
  전용). 장시간 작업은 AC 연결 확인 후 실행할 것.

## R9-1 결과: 정보 일관 determinization (2026-07-06)

구현: `ppo_env`가 상대 턴 이벤트(당시 테이블/멜드/드로우) 기록 →
`rollout_agent._sample_consistent()`가 "마지막 플레이 이후 연속 드로우 구간"
제약(각 턴 낼 조합 0개, pre-meld면 30 미달)으로 기각 샘플링. `--consistent`.

**결과 (160페어, meld=30, det=8, margin=0): 승률 46.9%, pair net -0.56±0.31 —
기준선(48.8%) 이하, fair(50.9%)보다 나쁨.** v1과 동일한 실패 모드로 진단:
margin 가드 없는 argmax + det=8의 손패 간 분산(semi는 손패가 고정이라 저분산).
추론 방향의 문제가 아니라 노이즈 관리의 문제로 판단. 후속: margin 가드 +
successive-halving으로 det 증액 후 재측정.

## 수 공간 전수조사 (2026-07-06)

"한 턴에 낼 수 있는 서로 다른 수"는 손패의 부분 멀티셋 중 테이블과 합쳐 유효
분할이 존재하는 것 (상대가 최적 재배열하므로 배열은 무의미, **남는 손패 dedupe가
정확히 옳음** — 같은 이유로 '배열 블로킹'은 재배열 상대에겐 존재하지 않는 채널).

실측 (12개 실전 포지션 전수 체크): **턴당 유효 수 = 중앙값 ~2, 대부분 한 자릿수,
최대 27** (테이블 큰 종반). solve_many(cap 20)가 못 담는 포지션 존재(27>20) &
반대로 배열 변형만 20개 생성하는 낭비도 확인. → **후보 생성을 휴리스틱 다양화
대신 전수 열거로 교체 가능**: 활성 타일(어떤 유효 세트에도 못 들어가는 타일 제외)
부분집합만 feasibility 체크하면 턴당 ~수십 ms. 생성 누락 문제 원천 해소.

**남은 R9 과제**: (1) 전수 열거 후보 생성기, (2) consistent + margin +
halving-det 재실험, (3) full oracle 패배 게임 부검(11.2%가 불가항력 딜인지
1-ply 한계인지 판별).

## 부검 결과 (2026-07-06, `autopsy_oracle.py`, 로그 `R8/autopsy_oracle.log`)

그리디 상대는 결정론적 → "이길 수 있었나"는 내 수 분기만 DFS하면 증명 가능.
full oracle이 진 10게임 전부에 대해 DFS 실행:

**판정: 10/10 WIN — 전부 이길 수 있는 게임이었음. NO_WIN 0.**
승리 경로는 40~340노드(수 초)에서 발견 = 2~3수 앞 탐색이면 잡히는 깊이.

함의:
- 완전정보 천장은 88.8%가 아니라 **~100%**. 88.8%는 1-ply 행동 방식의 천장.
- "불가항력 딜"은 이 표본에 존재하지 않음. 생성 누락도 원인 아님(같은 생성기로
  DFS가 이김). **병목 = 탐색 깊이.**
- semi-oracle 55.9%도 1-ply 측정치 → 손패 예측의 진짜 상한은 그보다 높을 수
  있음. 다음 실험: **semi-oracle + 2~3-ply 탐색** (determinization마다 소형
  DFS — 상대가 결정론적이라 가능). 유의하게 넘으면 fair 에이전트 설계를
  1-ply rollout → determinized 얕은 탐색으로 전환.
- 선행 조건: 속도 작업 (forced-draw 사전판정, 전수 열거 생성기, halving).

## 속도 작업 결과 (2026-07-06)

1. **전수 열거 생성기 완성** (`solver.enumerate_moves`, env `exhaustive_candidates`,
   eval `--exhaustive`): 163 실전 포지션 검증 — solve_many 대비 누락 0(상위집합),
   **+16% 수 발견 (272 vs 234)**, fallback 0. 단 포지션당 497ms vs 106ms로 5배
   느림 → 속도 수단이 아니라 **완전성 수단** (탐색 실험용).
2. **forced-draw 사전판정**: 구현·정합(회귀 통과)했으나 실측 적중 3~5%뿐.
   타입 수준 필요조건이 너무 약함 (큰 테이블에선 대부분 손패 타입이 어떤 유효
   세트에든 등장 → ILP까지 가서야 불가능 판명). 공짜라 유지하되 효과 미미.
3. **핵심 발견 — 진짜 병목 재특정**: solve 비용은 테이블 크기에 비례.
   빈 테이블 0.87ms → 테이블 29장 **10.9ms**. 그리고 실제 롤아웃 solve의
   **55%가 infeasible 증명**(낼 수 없음 확인)에 쓰임. 즉 최적화 대상은
   "빠른 불가능 판정 + 빠른 최적화" = **van Rijn & Takes DP 전환**이
   유일하게 남은 큰 지렛대 (기대 10~50×, 공사 1~2일).

다음 결정: (A) DP부터 구현 후 탐색 실험 (모든 후속 실험이 solve 비용의 배수라
선투자 가치 큼) vs (B) 현재 속도로 semi+얕은탐색 소규모(40페어) 선실험.

## DP 솔버 전환 완료 (2026-07-06, `rummikub_dp.py`)

- **구현**: 숫자 1→13 스윕, 상태 = 색깔별 열린 런 길이쌍(0/1/2/3+ 캡, 색당 최대
  2런), 그룹은 g-벡터(81종) 사전계산, 역추적으로 실제 배열 복원. 목적 = 테이블
  전량 사용 제약 하 손패 사용 최대화, min_play_value는 가치 차원(30 캡)으로 지원.
- **통합**: `RummikubILPSolver(use_dp=True)` 기본. 단순 max-play `solve()`와
  `enumerate_moves` feasibility는 DP, exclusion/exact-k(`solve_many`)는 ILP 유지.
- **부수 발견 — ILP 원죄 버그**: 결정변수가 Binary라 같은 세트 2회 선택 불가
  (타일 2벌!). 예: [R9,B9,Y9]×2가 필요한 최적해를 놓치고, 중복 세트를 포함한
  합법 테이블을 infeasible 처리할 수 있었음. **upBound=2 Integer로 수정.**
  DP와의 대조 검증에서 발견됨.
- **검증**: 실전+랜덤 266포지션 × (일반/초기등록) 532쿼리 불일치 0, 배열 전부
  유효, enumerate 60포지션 일치 0오차.
- **속도**: 핫패스(중반 max-play) 2.62ms → **0.10ms (26×)**. 혼합 평균 4×.
- **⚠ 기준선 무효화**: 그리디 상대가 버그 수정으로 강해짐 → 기존 sanity 48.8%
  / fair 50.9% / semi 55.9% 등 모든 수치는 새 실험과 직접 비교 금지.
  DP 기준 재측정 필요 (재측정 자체는 이제 수 분).
- **파라노이드 대조 모드**: 매 100번째 DP solve를 ILP와 실시간 대조
  (`dp_crosscheck_every`), 불일치 시 즉시 RuntimeError. 오류 주입 테스트 통과.

## DP 시대 새 기준선 (2026-07-06/07, 40페어, 시드 2000~2039, meld=30)

| 실험 | 승률 / pair net | 속도 |
|---|---|---|
| greedy sanity | 47.5% / -0.30±0.39 | 1.23s/게임 |
| fair rollout v2 | 46.2% / -0.57±0.51 | 3.05s/게임 (구 10.2s) |
| **semi-oracle** | **53.8% / +0.47±0.54** | **3.01s/게임 (구 33s, 11×)** |

- semi − sanity = **+6.3%p** — 강해진 상대 기준에서도 구 시대의 +7.2%p 갭이
  재확인됨. **"손패 지식의 가치" 결론은 솔버 교체 후에도 유지.**
- 다음: 3단계 실험 (semi + 엔드게임 제한 DFS — 1-ply 천장 검증) 설계 승인 대기.
  설계: det별 노드 400 DFS, 트리거 = 덱 ≤12 or 손패 ≤6, 노드 확장은
  enumerate_moves(DP). 구현 2~3h + 파일럿 5페어 + 본실험 40페어(~15분 예상).

## R9-3 결과: 엔드게임 승리강제 탐색 (2026-07-07, `R8/semi_endgame_search_40.log`)

구현(`rollout_agent`): `endgame_search=True`면 어느 쪽 손패 ≤5장일 때
determinization마다 노드 120 제한 DFS로 "이 수가 승리를 강제하는가" 판정,
강제승 발견 시 1-ply 점수 무시하고 투표로 선택. 탐색 노드의 수 생성은 손패
≤8장이면 전수 열거(DP), 크면 solve_many 4개 (첫 파일럿은 트리거 덱≤12 +
전수열거 무제한으로 발산 → 손패 트리거로 축소해 해결. 97.6s/게임 순차).

**결과 (40페어, 시드 2000~2039): 승률 57.5%, pair net +0.25±0.50, WW12/LL6.**
- semi 1-ply(53.8%) 대비 +3.7%p (~1σ, 160페어 확정 필요), sanity(47.5%) 대비
  +10%p (~2σ). **부검의 "1-ply 인공 천장" 가설을 지지 — 행동 깊이 채널 실재.**
- 개선 예산 지도: 손패 지식 +6.3%p, 엔드게임 깊이 +3.7%p (중첩 가능).
- 다음 후보: (A) semi+탐색 160페어 유의성 확정 (~1.5h), (B) fair 조합
  실험 = consistent+margin+엔드게임탐색 40페어 (~40분) — 최종 목표 지표.

## HiGHS presolve 버그 사건 (2026-07-07 새벽)

- B→A 체인 실행 중 **파라노이드 대조 모드가 실전 불일치를 포착**: DP=3 vs ILP=2
  (시드 2098 게임). DP 배열은 유효(구성적 증명) → ILP가 오답.
- 원인 분리: 같은 수식에서 CBC=3, HiGHS(presolve on)=2, HiGHS(presolve off)=3.
  → **HiGHS 1.13.1 presolve가 일반 정수(upBound=2) 모델에서 잘못된 최적해를
  "Optimal"로 반환하는 버그.** upBound=2 수정(어제) 전에는 Binary라 미발현.
- 조치: `make_default_lp_solver()`에 `presolve='off'` (ILP ~13ms로 느려졌지만
  ILP는 이제 solve_many/crosscheck 전용). 신규 118포지션 재검증 0 불일치.
- **오염 평가**: upBound=2 이후 측정된 DP 시대 기준선들(sanity 47.5 / fair 46.2
  / semi 53.8 / semi+search 57.5 / fair combo 55.0)은 에이전트 측 obs 후보
  생성(solve_many→HiGHS presolve)이 드물게 오염된 상태로 측정 → 전 스위트
  재측정 필요 (효과는 작을 것으로 추정되나 위생상 필수).
- 교훈: 대조 모드가 하루 만에 본전을 뽑음. 유지.
- **업스트림 조사 (2026-07-07)**: pulp 우회 순수 재현(MPS) 확보 — presolve on/off가
  같은 파일에서 -58/-59를 모두 "Optimal" 주장. GitHub에 유사 이슈 #2806(1.13 MIP
  조기 종료, open) 존재. **1.14.0에서 우리 재현은 수정 확인** (임시 env 테스트)
  → 신규 보고 불필요. 단 1.14는 별개 MIP 버그(#2957, 1.15에서 수정) 전력이 있어
  **당분간 1.13.1 + presolve=off 유지**, conda-forge에 1.15.1 올라오면 업그레이드.
  재현 파일: scratchpad/highs_asbuilt.mps (세션 임시 경로).

운영 메모: 장시간 벤치마크는 `caffeinate -is`로 걸 것 — 클램셸 슬립으로 밤새
체인이 12시간 밀린 사례 있음(2026-07-03).

## presolve=off 재측정 + semi+탐색 160페어 확정 (2026-07-07, `R8/rerun_*.log`)

전 스위트 재측정 (40페어, 시드 2000~2039, meld=30, presolve=off 위생 상태):

| 실험 | 승률 / pair net | 구(오염) 수치 |
|---|---|---|
| greedy sanity | 46.2% / -0.45±0.38 | 47.5% |
| fair rollout v2 | 45.0% / -0.53±0.57 | 46.2% |
| semi-oracle 1-ply | 52.5% / -0.12±0.44 | 53.8% |
| semi + 엔드게임 탐색 | 56.2% / +0.72±0.56 | 57.5% |
| fair combo | 55.0% / -0.10±0.65 | 55.0% |

- **오염 영향 미미 확인**: 전 실험 1~1.5%p 이내 재현, 순서 불변. 기존 결론 유지.
- **semi+탐색 160페어 확정** (`R8/semi_search_ext120.log`, 신규 120페어 61.3% /
  +0.82±0.30): **풀링 160페어 = 승률 60.0% (192/128), pair net +0.80±0.27 (~3σ),
  WW51/LL19.** 독립 표본에서 재현·강화 — **그리디를 유의하게 이기는 첫 에이전트
  확정.** 손패 지식(+6.3%p)과 엔드게임 깊이 채널의 중첩 확증.
- fair combo(배치 가능 에이전트)는 승률 +8.8%p이나 pair net 중립(-0.10),
  40페어 비유의(WW10/LL6). semi와 격차 2.5%p뿐 — 정보 일관 샘플링이 오라클
  가치의 상당 부분을 회수하는 것으로 보임. **다음: fair combo 120페어 확장
  (~65분)으로 유의성 판정.**

## fair combo 160페어 확정 (2026-07-07, `R8/fair_combo_ext120.log`)

신규 120페어(시드 2040~2159): 승률 57.5%, pair net +0.64±0.36, WW39/LL21.
**풀링 160페어: 승률 56.9% (182/320), pair net +0.46±0.31 (1.5σ),
WW49/LL27 (부호검정 2.5σ, p≈0.012).**

- **치팅 없는(배치 가능) 에이전트의 첫 유의 우위.** 승률/부호검정 기준 유의,
  단 pair net 마진은 얇음(semi+탐색의 +0.80/3σ보다 약함) — "유의하게 이기나
  근소하게 이긴다". 중간(51페어 51.0%)에서 후반 강회복 — 페어 노이즈 큼.
- **정보 일관 샘플링의 회수율 높음**: semi+탐색(60.0%)과 3.1%p 격차.
  R9-1 단독 실패(46.9%)였던 consistent가 margin+엔드게임탐색과 결합해 부활.
- 예산 지도: sanity 46.2 → fair combo 56.9 (+10.7) → semi+탐색 60.0 (+3.1,
  손패 추정 개선 몫) → full oracle ~89 (덱 예지, 회수 불가).
- 구성: `--consistent --greedy-margin 1.0 --endgame-search`, det=8,
  rollout-turns 24, cap 4. 페어당 ~40s (workers 8).

## 작업 규칙

- 라운드(R) 단위로 진행하고, 라운드가 끝나면 README/CLAUDE.md의 해당 섹션을 갱신.
- 에이전트 간 비교는 반드시 미러 페어 평가로. 단판 승률 비교 금지.
- 학습 로그/모델은 `R<N>/` 디렉토리에 보관 (기존 관례).
