# AID Rummikub — 전략 재정립 및 R10(증류) 플랜

## Context

### 무엇을 하려는가 (목표, 2026-07-07 확정)

- 2인 루미큐브(조커 없음, meld=30)에서 그리디 ILP 베이스라인을 **학습된 에이전트**로 유의하게 이긴다.
- 요건 엄격도는 단계적: **수준 2 (하이브리드) 먼저 달성 → 최종 목표는 수준 3 (순수 네트워크)**.
  - 수준 2: 수 선택 주체 = 네트워크. 솔버는 합법 후보 열거만. 손패 ≤5 종반 승리강제 DFS는 보조 계산기로 허용. 단 "네트워크 기여가 유의함"을 ablation으로 증명해야 함.
  - 수준 3: 플레이 시점 = 후보 열거 + forward pass만. 종반 판단까지 네트워크가 담당.
- 성공 판정선: ① 그리디 유의 우위(160페어+ 미러, 부호검정/pair net) ② 선생(fair combo 56.9%) 수준 근접 또는 초월 ③ 랜덤 압도.

### 지금 어떤 상황인가 (확보된 사실)

- **이기는 선생은 확보됨**: fair combo(consistent + margin + 엔드게임 DFS, 학습 파라미터 0개) = 160페어 승률 56.9%, 부호검정 2.5σ. semi-oracle+탐색(손패 치팅) = 60.0%, 3σ.
- **예산 지도**: sanity 46.2 → fair combo 56.9 (+10.7) → semi+탐색 60.0 → full oracle ~89 (덱 예지, 원리적 회수 불가).
  - ⚠ 단 semi−fair 갭 +3.1%p는 페어드 재분석 결과 **0.9σ로 미확립** — "손패 추정 개선 몫"은 검증 전 가정.
- **인프라**: DP 솔버(0.1ms), 미러 페어 평가(+페어드 비교), 전수 열거 생성기, 부검 도구. 실험 회전 속도 빠름.
- **R1–R7 교훈**: dense 보상 RL은 그리디 모방으로 수렴(~50% 고정점). 신호 검증 전 학습 투자 금지.

### 식별된 핵심 리스크 (비판적 분석 결과)

1. **희소 라벨**: 선생은 대부분의 턴에서 그리디와 동일한 수 → 순진한 증류는 "그리디 복사기"(~46%)가 될 위험. 이탈 결정(엣지의 원천)은 게임당 소수.
2. **종반 조합 정밀성**: 승리강제는 한 수 오차로 무효 — 함수 근사가 가장 어려운 부분. 수준 3의 최대 난관.
3. **네트워크 표현력 미검증**: 이 구조(state+cand scorer)가 그리디 수준의 결정 함수라도 담을 수 있는지 아직 모름 (R1–R7은 RL 노이즈와 교락되어 판정 불가).
4. **히스토리 인코딩 부재**: 암묵적 손패 추론에는 상대 턴 이벤트 시퀀스 관측이 필요한데 현재 obs는 108차원 스냅샷.

## Plan — 단계적 증류 (R10)

리스크를 아래층부터 격리 검증하는 사다리. 각 단계는 이전 단계의 성공을 전제.

### Stage 0: 그리디 증류 — 표현력 리스크 격리 (리스크 3)

- 데이터: greedy vs greedy 자가대전(게임당 ~1.2s → 시간당 수천 게임). 각 결정에서 (obs, 후보 목록, 그리디의 선택) 기록. 라벨 충돌 0.
- 학생: `ppo_model.py`의 기존 구조(state 인코더 + 후보 인코더 + score head) 재사용, cross-entropy 지도학습.
- 판정: 학생 단독 미러 평가가 그리디 sanity(46.2%)와 동급이면 통과. 실패 시 구조 교체(용량 증대, attention 등)를 여기서 해결 — 위층 투자 전에.

### Stage 1: fair combo 선생 증류 — 수준 2 달성 (리스크 1)

- **데이터 파이프라인** (Stage 0과 공용 설계): fair combo vs greedy 게임에서 (obs, 후보, 선생 선택, **그리디의 선택**, 상대 손패 라벨, 상대 턴 이벤트 히스토리) 저장. 선생 게임 ~160s/게임 → 8워커 하룻밤 ≈ 2,000게임(~4만 결정).
  - 그리디 선택을 함께 저장하는 이유: 이탈 결정 식별 + 가중치 부여의 근거.
- **이탈 가중 학습**: 선생 ≠ 그리디인 결정을 오버샘플/가중. 가중 강도는 스윕(과적합 반대 리스크 관리).
- **보조 헤드**: 상대 손패 분포 예측(52차원) — 라벨 공짜, DouZero+식 표현 학습 보조. ablation으로 효과 측정.
- **플레이 형태 (수준 2)**: 미드게임 = 학생 argmax, 손패 ≤5 = 기존 엔드게임 DFS.
- **판정 (160페어 미러, 페어드)**:
  - 필수: 그리디 유의 우위 (성공선 ①)
  - ablation A: **그리디 + 엔드게임 DFS만** 베이스라인 대비 학생의 한계 기여 유의 → "학습된 에이전트" 자격 증명
  - 목표: 선생 56.9%에 근접 (성공선 ②)

### Stage 2: 선생 개선 루프 — expert iteration (여유 시간 활용)

- 학생이 선생에 근접하면: 학생을 롤아웃 리프 평가/플레이아웃에 활용해 선생 강화 → 재라벨 → 재증류.
- 히스토리 인코딩 업그레이드(리스크 4): 보조 헤드가 효과를 보이면 상대 턴 이벤트 시퀀스 인코딩(고정 길이 요약 또는 소형 시퀀스 인코더)에 투자.
- 보류 중인 oracle guiding(semi 선생 증류)은 **semi−fair 갭이 유의해질 때만 부활** — 선결 실험: semi vs fair 320페어 페어드 확장(~수 시간).

### Stage 3: 종반 네트워크화 — 수준 3 도전 (리스크 2)

- 핵심 관찰: 손패 ≤5 종반 포지션은 상태 공간이 작고 DFS로 **정답 라벨을 무한정 생성 가능** (승리강제 여부 + 강제 수). 별도 종반 데이터셋 대량 생성 → 종반 전용 학습(같은 네트워크의 커리큘럼 or 종반 헤드).
- 판정: 순수 네트워크(DFS 제거)가 하이브리드 대비 손실 ε 이내 & 그리디 유의 우위 유지.
- 실패 시에도 수준 2 결과는 확보된 상태 — 최종 보고에 "종반 근사의 한계" 분석으로 수록.

### 기각/보류된 대안 (기록)

- **RL from scratch (PPO 계속)**: R1–R7에서 실패 원인 규명 완료. 기각.
- **ReBeL/Student of Games**: 균형 계산 기계 — 고정 결정론 상대 착취라는 우리 문제와 불일치, 행동 공간 궁합 나쁨. 자가대전 단계에서 재검토.
- **명시적 손패 예측기 우선 노선**: 증류의 보조 헤드로 흡수. 갭 유의성 확립 시 승격 가능.
- **oracle guiding (semi 선생 증류)**: 전제(갭)가 0.9σ로 미확립 + 라벨 충돌이 엣지 집중 지점에 발생. Stage 2의 조건부 항목으로 강등.

## 구현 순서 (Stage 0–1 공사)

1. **리포 문서화**: 이 전략을 `ROADMAP.md`로 리포에 커밋 (CLAUDE.md에는 링크 + 요약).
2. **데이터 파이프라인** (`selfplay_data.py` 신규): eval_mirror의 게임 루프 재사용, 결정 레코드(JSONL/NPZ) 저장. 그리디 모드(Stage 0)와 선생 모드(Stage 1) 공용. 상대 손패·이벤트 히스토리 라벨 포함.
3. **학습 스크립트** (`distill.py` 신규): ppo_model 구조 로드, CE + 보조 헤드 loss, 이탈 가중 옵션.
4. **평가 통합**: `eval_mirror.py`에 `--policy student --model <pt>` 추가 (forward-only 정책).
5. Stage 0 실행: 데이터 생성(수 시간) → 학습(분) → 40페어 스모크 → 160페어 판정.
6. Stage 1 실행: 선생 데이터 하룻밤 생성 → 학습 스윕 → 판정 + ablation 2종.

## Verification

- 모든 비교는 160페어+ 미러, 같은 시드셋(2000~2159)으로 페어드 비교 + 부호검정. 단판/소표본 비교 금지 (작업 규칙).
- Stage 0 게이트: 학생 ≈ greedy sanity. Stage 1 게이트: 그리디 유의 우위 + ablation A 유의. Stage 3 게이트: 순수 넷 ≥ 하이브리드 − ε.
- 장시간 작업은 AC 전원 확인 + `caffeinate -is`, 시작 시 ETA 보고.
